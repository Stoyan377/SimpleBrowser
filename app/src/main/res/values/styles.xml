<resources>

    <!-- Base application theme using Material Design 3 -->
    <style name="AppTheme" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/surface</item>
    </style>

    <!-- URL Input Style -->
    <style name="URLInputStyle">
        <item name="android:textColorHint">@color/url_bar_hint</item>
        <item name="android:textColor">@color/url_bar_text</item>
        <item name="android:background">@drawable/url_input_background</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Button Style -->
    <style name="ModernButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/text_light</item>
        <item name="backgroundTint">@color/primary</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- Bottom App Bar Style -->
    <style name="BottomBarStyle">
        <item name="android:background">@color/bottom_bar_surface</item>
    </style>

</resources>
